generator client {
    provider = "prisma-client-js"
}
datasource db {
    provider = "sqlite"
    url = env("DATABASE_URL")
}
model jogador{
    id Int @id @default(autoincrement())
    nome String @unique
}
model selecao {
    id Int @id @default(autoincrement())
    nome String @unique
}